export { Thumbnail };
class Thumbnail {
    constructor(source, defaultSource, dimensions, width, height) {
        this.source = source;
        this.defaultSource = defaultSource;
        this.dimensions = dimensions;
        this.width = width;
        this.height = height;
    }
}
